# Testing requirements for Loca integration
# Note: pytest-homeassistant-custom-component hard-pins the pytest stack
# (pytest, pytest-asyncio, pytest-cov, pytest-xdist) and homeassistant, so the
# floors below match those pins. mypy, ruff and pytest-mock are independent.
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-homeassistant-custom-component>=0.13.339
pytest-mock>=3.15.1
pytest-cov>=7.1.0
pytest-xdist>=3.8.0

# Type checking and linting
mypy>=2.1.0
ruff>=0.15.17

# Home Assistant core for testing (minimum supported baseline;
# pytest-homeassistant-custom-component pulls the exact tested version)
homeassistant>=2026.3.0
